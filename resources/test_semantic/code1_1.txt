programa exemplo;

inicio
    var x: int;
    x => 10;

    var a: bool;
    a => verdadeiro;

    var b: int;
    b => 5 + 3;

    funcao soma(x: int, y: int) retorna int;
    inicio
        retorna x + y;
    fim

    var resultado: int;
    resultado => chamar soma(5, 2);

    funcao retornar_int(dummy: int) retorna int;
    inicio
        retorna 42;
    fim

    var cond: bool;
    cond => verdadeiro;
    se (cond) entao
        mostre (1);
    fim
fim