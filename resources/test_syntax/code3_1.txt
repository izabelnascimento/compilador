programa RECURSIVO;
inicio
    var hi: int;
    hi => 3;
    funcao fatorial (n: int) retorna int;
    inicio
        se (n == 0) entao
            retorna 1;
        senao
            retorna n * chamar fatorial(n - 1);
        fim
    fim
    mostre(chamar fatorial(5));
    mostre(hi);
fim
